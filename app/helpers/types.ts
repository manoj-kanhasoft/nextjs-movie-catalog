// Interface definition for the request to get movies
export interface getMoviesRequest {
  // Optional token parameter that can be a string, undefined, or null
  token?: string | undefined | null;

  // Add other properties as needed
  // For example, you can include additional parameters relevant to the getMovies request
  // For instance: pageNumber?: number;
  // Feel free to customize this interface based on your specific requirements
}
